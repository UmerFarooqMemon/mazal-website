# Dashboard assets

Image fills downloaded from Figma REST (`FIGMA_TOKEN`, no MCP, no full-page screenshots):

- `assets/*.png` — plate / decorative fills used by dashboard frames

Drop your own full-page Figma screenshots in:

`figma-dump/dashboard-screenshots/`
